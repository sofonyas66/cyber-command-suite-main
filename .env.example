VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-publishable-key

# For server-side functions (Deno / Supabase Edge Functions) set the service role key.
# This must NEVER be exposed to the browser or committed.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Example Splunk integration vars (update if you wire Splunk next)
# VITE_SPLUNK_HEC_URL=https://splunk.example.com:8088
# VITE_SPLUNK_HEC_TOKEN=your-splunk-token

# Copy this file to `.env` and fill values before running the app.